/**
 * Placeholders (unconfirmed metrics, missing diagrams, projects without a case) show in every
 * environment while the site is behind its password. Set to false to hide them when the site opens.
 */
export const showPlaceholders = true;
